const { makeStyles } = require("@material-ui/styles")

export const useStyles = makeStyles({
    appContainer: {
        height: "100vh",
    },

    headerSection: {
        height:"10vh"
    },

    corouselSection: {
       height:"90vh"
    }

});